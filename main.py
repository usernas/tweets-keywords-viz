#!chemin
"""
Twitter API will be use to search for tweets containing a single keyword given by the user and 
then extract more keywords that frequently appears in these tweets.
 And i will finally visualize these datas on webpage as a point cloud or using js animation.
"""

import os

rep = input("Entrez le mot-clé à analyser : ")
rep = str(rep)
print(rep)
#test